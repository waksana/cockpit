{
  "name": "@cockpit/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API-only MCP client for Cockpit sessions, capabilities, and file transfers.",
  "bin": {
    "cockpit-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@cockpit/protocol": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "tsx": "^4.23.13"
  }
}
