{
  "name": "@cockpit/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Maintainer-only MCP server over cockpit's session trash bin (list/read/restore/purge).",
  "bin": { "cockpit-mcp-server": "dist/index.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "node --import tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2"
  }
}
