# 部署记录

本文只记录**经过认证回执及实际运行回读的事实**，不是持续实时状态、待发布清单或
自动部署授权。运行版本仍以 `/version`、同实例 `/health` 和原 `requestId` 的交付回执
为准。流程见[私有交付](DELIVERY.md)，能力变化见[模块目录](module-catalog.md)。

## 2026-09-13：薄本体上线

用户在 18:02（UTC+08:00）明确要求部署后重启。此前的普通重启只重新启动旧版本，
没有部署新源码；本次通过既有外部控制器完成独立的固定 SHA 部署。

| 证据 | 值 |
| --- | --- |
| 完成时间 | 2026-09-13 18:10:48（UTC+08:00） |
| 源码 SHA | `a1f4a9a7c9e72b151958c270c4f790b32b72636e` |
| 原请求 ID | `cockpit-thin-core-a1f4a9a7-20260913-1802` |
| 交付结果 | `succeeded`，无 failure/rollback |
| 运行包 SHA-256 | `3dafcfcae7a9510ecdf4b8b54de15227215dfa2f520b0c392a415d702ced6cf5` |
| 构建配置 SHA-256 | `2b3cd6fd99c1a0d2c9fb8d93beaf9a9364b4eb9a1ac93f48d6e29b6ce8a3e1d6` |
| 固定构建 | [Delivery CI 34751006165](https://github.com/waksana/cockpit/actions/runs/34751006165) |
| 运行实例 | `26f4b562-9d50-494a-a7d3-578348d73195` |
| 替换前源码 | `c93497877c7a4029251a09a8eec1f36b75bcd144` |

回读覆盖新 SHA/产物摘要、同一新实例健康，以及实际 Web index/入口资源与已安装
manifest 一致。该版本暴露 46 个原生产品 intents，迁出的文件/通知/语音/置顶/
自动命名/模块接入路径不再注册。匿名公网入口仍经过认证保护，Task 和微信仍处于
停用状态，没有借本次部署恢复它们。

此次只切换主程序代码，不清理或迁移既有用户资料，不 reset/删除原生 session，
不重放 unknown 业务操作。模块原件仍未适配成可安装插件。

主包显示版本仍为 `0.1.0`，所以这个字符串不能唯一证明运行了哪次更改：
必须同时看源码 SHA、产物摘要、请求和实例。此包是已授权私有部署的 CI 产物，
不是已经签名发布到消费者 Release 的证明。

## 记录维护

源码提交、文档提交、push CI 成功和“restart pending”不新增成功部署记录。
只有新授权请求完成运行回读后才追加一节；不能把旧请求改写成另一个 SHA 已部署，
也不能因当前 `main` 前进而修改历史实例。

更早部署的原始记录可从
[清理前的交付文档](https://github.com/waksana/cockpit/blob/a1f4a9a7c9e72b151958c270c4f790b32b72636e/docs/DELIVERY.md)
追溯；其中“当前安装”等措辞只属于当时，不是本页之外的另一份实时状态。
私人审批、凭据、控制器数据库和完整运行日志不进入此公开文档。
